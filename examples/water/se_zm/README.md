# Input for SeZM: Smooth equivariant Zone-bridging Model (PyTorch)

This directory stores a minimal configuration for training SeZM
(`model.type: SeZM`, `descriptor.type: SeZM`) on the water example dataset.

Run:

```bash
cd examples/water/se_zm
dp --pt train input_torch.json
```
